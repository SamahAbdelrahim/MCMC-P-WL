# Upload file -> Wait for upload to complete automatically
# Click the download icon (span.x.ih)
# -> This opens format options
# Choose MP4 in the select box (id="qi")
# Click the visible "Convert" button (a.i.xh)
# Wait for the final "Download" link (a.ja.gn)
# Click the final "Download" link
# Wait for the mp4 to appear in download folder
